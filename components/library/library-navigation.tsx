import Link from "next/link";

export function LibraryNavigation({ basePath, archived, page, hasNext }: { basePath: string; archived: boolean; page: number; hasNext: boolean }) {
  const view = archived ? "archived" : "active";
  return <div className="flex flex-wrap items-center justify-between gap-3"><div className="flex rounded-lg border border-line bg-surface p-1"><Link href={basePath} className={`rounded-md px-3 py-1.5 text-sm ${!archived ? "bg-ink text-white" : "text-ink-soft"}`}>Active</Link><Link href={`${basePath}?view=archived`} className={`rounded-md px-3 py-1.5 text-sm ${archived ? "bg-ink text-white" : "text-ink-soft"}`}>Archived</Link></div><div className="flex gap-2"><Link aria-disabled={page <= 1} href={page <= 2 ? `${basePath}${archived ? "?view=archived" : ""}` : `${basePath}?view=${view}&page=${page - 1}`} className={`rounded-lg border border-line px-3 py-1.5 text-sm ${page <= 1 ? "pointer-events-none opacity-40" : "hover:bg-ink/5"}`}>Previous</Link><Link aria-disabled={!hasNext} href={`${basePath}?view=${view}&page=${page + 1}`} className={`rounded-lg border border-line px-3 py-1.5 text-sm ${!hasNext ? "pointer-events-none opacity-40" : "hover:bg-ink/5"}`}>Next</Link></div></div>;
}
